export const fontSizes = {
  xs: 12,  // extra small (helper text, captions)
  sm: 14,  // small (labels, inputs, secondary text)
  md: 16,  // medium (default body text)
  lg: 20,  // large (subheadings, buttons)
  xl: 24,  // extra large (headings, titles)
  xxl:28
};
